#! @BASH@ -e

if test -e gras/trp_tcp_server@EXEEXT@ ; then
  base=gras
else 
  base=.
fi

if [ x@EXEEXT@ = x ] ; then 
  wine=
else
  wine=wine
fi

$wine $base/trp_tcp_server@EXEEXT@ $@ &
sleep 1
$wine $base/trp_tcp_client@EXEEXT@ $@

