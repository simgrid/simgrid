#! ./tesh
# This test detects a global timeout

! expect return $ELEADTIME

< $ sleep 10
$ ./tesh --log="log.thresh:info tesh.fmt:%m%n" --timeout=1
> Test unit from stdin
> [stdin:1] sleep 10
> Test unit from stdin INTR
> Tesh timed out after `(1)' seconds
