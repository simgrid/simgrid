never { /* !(!(GFdeliver)) */
T0_init :    /* init */
	if
	:: (deliver) -> goto accept_S1
	:: (1) -> goto T0_init
	fi;
accept_S1 :    /* 1 */
	if
	:: (deliver) -> goto accept_S1
	:: (1) -> goto T0_init
	fi;
}