#! @BASH@ -e

if test -e gras/trp_file_server@EXEEXT@ ; then
  base=gras
else 
  base=.
fi

if [ x@EXEEXT@ = x ] ; then 
  wine=
else
  wine=wine
fi

$wine $base/trp_file_client@EXEEXT@ $@ | $wine $base/trp_file_server@EXEEXT@ $@ 

